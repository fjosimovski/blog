<% if @session.errors.any? %>
  <div id="errors">
    <h4 class="alert-danger"><%= pluralize(@user.errors.count, "error") %> prohibited this task from being saved:</h4>

    <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>